A simple Golang websocket chat.

Dependencies:
- chi router
- gorilla's websocket
- apex/log

To build:
- `go build -o srv ./cmd/server/`
- `go build -o client ./cmd/client/`

Launch server:
- `./srv`

Launch client:
- `./client`
